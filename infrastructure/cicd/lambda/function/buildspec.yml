version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.12

  build:
    commands:
      - |
        cd src/web
        zip -r ../../function.zip app config.py lambda_function.py

artifacts:
  files:
    - 'function.zip'